<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.hj.mdanimatordemo">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".transitionManager.BeginDelayedTransitionActivity"></activity>
        <activity android:name=".transitionManager.ChangeImageTransformActivity" />
        <activity android:name=".transitionManager.ChangeClipBoundsActivity" />
        <activity android:name=".transitionManager.ChangeTransformActivity" />
        <activity android:name=".transitionManager.ChangeBoundsActivity" />
        <activity android:name=".transitionManager.AutoTransitionActivity" />
        <activity
            android:name=".SevenActivity"
            android:theme="@style/AppTheme.WindowTransition" />
        <activity
            android:name=".SixActivity"
            android:label="SixActivity" />
        <activity android:name=".FiveActivity" />
        <activity android:name=".FourthActivity" />
        <activity android:name=".ThirdActivity" />
        <activity
            android:name=".SecondActivity"
            android:theme="@style/SecondTheme" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>